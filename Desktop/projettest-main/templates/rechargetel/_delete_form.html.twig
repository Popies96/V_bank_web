<form method="post" action="{{ path('app_rechargetel_delete', {'idRech': rechargetel.idRech}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rechargetel.idRech) }}">
    <button class="btn">Delete</button>
</form>

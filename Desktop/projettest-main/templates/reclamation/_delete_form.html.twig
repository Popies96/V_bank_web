<form method="post" action="{{ path('app_reclamation_delete', {'idReclam': reclamation.idReclam}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.idReclam) }}">
    <button class="btn">Delete</button>
</form>

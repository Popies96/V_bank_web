<form method="post" action="{{ path('app_demandecheque_delete', {'numerocompte': demandecheque.numerocompte}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandecheque.numerocompte) }}">
    <button class="btn">Delete</button>
</form>

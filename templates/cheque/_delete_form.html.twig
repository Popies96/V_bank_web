<form method="post" action="{{ path('app_cheque_delete', {'numero_de_cheque': cheque.numerodecheque}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cheque.numerodecheque) }}">
    <button class="btn">Delete</button>
</form>

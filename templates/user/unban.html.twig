<form method="post" action="{{ path('app_user_unban', {'idUser': user.idUser}) }}" onsubmit="return confirm('Are you sure you want to unban this User account?');">
    <input type="hidden" name="_token" value="{{ csrf_token('unban' ~ user.idUser) }}">
    <button class="btn">Unban</button>
</form>

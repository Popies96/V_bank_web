{% extends 'base.html.twig' %}

{% block title %}Marche{% endblock %}

{% block body %}
    <h1>Marche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marche.id }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ marche.userId }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ marche.quantite }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ marche.total }}</td>
            </tr>
            <tr>
                <th>DateAchat</th>
                <td>{{ marche.dateAchat ? marche.dateAchat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marche_index') }}">back to list</a>

    <a href="{{ path('app_marche_edit', {'id': marche.id}) }}">edit</a>

    {{ include('marche/_delete_form.html.twig') }}
{% endblock %}

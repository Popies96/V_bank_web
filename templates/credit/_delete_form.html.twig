<form method="post" action="{{ path('app_credit_delete', {'idDemande': credit.idDemande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ credit.idDemande) }}">
    <button class="btn">Delete</button>
</form>

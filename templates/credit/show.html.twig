{% extends 'base.html.twig' %}

{% block title %}Credit{% endblock %}

{% block body %}
    <h1>Credit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Status</th>
                <td>{{ credit.status }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ credit.montant }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ credit.date ? credit.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Historique</th>
                <td>{{ credit.historique }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_credit_index') }}">back to list</a>

    <a href="{{ path('app_credit_edit', {'idDemande': credit.idDemande}) }}">edit</a>

    {{ include('credit/_delete_form.html.twig') }}
{% endblock %}

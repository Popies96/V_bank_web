<form method="post" action="{{ path('app_demandedecredit_delete', {'cin': demandedecredit.cin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandedecredit.cin) }}">
    <button class="btn">Delete</button>
</form>
